/** Latest GitHub release — used for CLI download links in the designer. */
export const CLI_RELEASES_URL = 'https://github.com/mzworthington/blueprint/releases/latest';

/** Example headless scan command shown in onboarding UI. */
export const CLI_SCAN_COMMAND = 'blueprint --headless --output=blueprints';
