// Public API for the workspace feature.
// Only exports what external modules (App.tsx, router) are permitted to consume.
// All sub-components and hooks are private to this feature.
export { WorkspacePage } from './WorkspacePage';
